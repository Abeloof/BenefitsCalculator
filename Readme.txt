
Required to run & test app
    docker compose
Suggested to run & debug app
    docker compose
    VSCode with devcontainer plug-in

To run appliction
    git clone {{repo link}}
    cd BenefitsCalculator
    docker compose -f docker-compose.yml up


To debug application (VSCode)
    git clone {{repo link}}
    cd BenefitsCalculator
    code .
    